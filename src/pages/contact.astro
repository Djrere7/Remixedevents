---
import Base from '../layouts/Base.astro';
import Faq from '../components/Faq.astro';
import { site, awards } from '../data/site';

const contactFaqs = [
  { q: 'How do I book a consultation?', a: 'Use the Schedule Your Free Consultation button to book a call with REmixed Events.' },
  { q: 'Do you travel for events?', a: 'Yes. REmixed Events is based in Miami Beach and serves South Florida, destination events, and celebrations beyond.' },
  { q: 'What types of events do you service?', a: 'REmixed Events services luxe weddings, brand and influencer events, yacht parties, boat parties, private celebrations, corporate moments, proms, galas, and special events.' },
];
---

<Base
  fullTitle="Book a Miami Female DJ | Schedule a Free Consultation"
  description="Schedule a free consultation with REmixed Events for Miami female DJs, wedding entertainment, brand events, influencer events, and special celebrations."
>
  <section class="ct-hero">
    <div class="ct-glow glow" aria-hidden="true"></div>
    <div class="container wide ct-grid">
      <div class="ct-copy">
        <p class="kicker" data-reveal>Let’s talk</p>
        <h1 class="h1" data-reveal style="--reveal-delay:80ms">
          Tell us what you’re <span class="font-stroke gtext">planning.</span>
        </h1>
        <p class="ct-sub" data-reveal style="--reveal-delay:120ms">Schedule a free consultation today.</p>
        <p class="lede" data-reveal style="--reveal-delay:160ms">
          Ready to talk through your event? Book a free consultation with REmixed Events and let’s see how our
          female DJ entertainment team can bring the music, style and energy to your celebration. We reply quickly,
          keep the process simple and help you figure out exactly what you need.
        </p>

        <div class="ct-cta" data-reveal style="--reveal-delay:220ms">
          <a href={site.calendly} target="_blank" rel="noopener" class="btn btn-primary btn-lg">Schedule Your Free Consultation <span class="arrow">→</span></a>
        </div>

        <div class="ct-direct" data-reveal style="--reveal-delay:260ms">
          <a href={`mailto:${site.email}`} class="tlink">{site.email} <span class="arrow">→</span></a>
          <a href={site.phoneHref} class="tlink">{site.phone} <span class="arrow">→</span></a>
        </div>

        <ul class="ct-badges" data-reveal style="--reveal-delay:320ms">
          {awards.slice(0, 3).map((a) => <li class="chip">✦ {a}</li>)}
        </ul>

        <div class="ct-social" data-reveal style="--reveal-delay:360ms">
          <h3 class="ct-social-h">See more of what we do — <span class="gtext">follow us.</span></h3>
          <p class="ct-social-copy">Head to our <strong>Instagram</strong> for the full show: real weddings, brand events, packed dance floors, and behind-the-scenes.</p>
          <div class="ct-social-grid">
            <a href={site.socials.instagram} target="_blank" rel="noopener" aria-label="REmixed Events on Instagram" class="ct-social-item ct-social-item-lead">
              <span class="ct-social-icon">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163C8.741 0 8.332.014 7.052.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/></svg>
              </span>
              <span class="ct-social-handle">@remixedeventsllc</span>
            </a>
            <a href={site.socials.facebook} target="_blank" rel="noopener" aria-label="REmixed Events on Facebook" class="ct-social-item">
              <span class="ct-social-icon">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
              </span>
              <span class="ct-social-handle">Remixed Events</span>
            </a>
            <a href={site.socials.linkedin} target="_blank" rel="noopener" aria-label="REmixed Events on LinkedIn" class="ct-social-item">
              <span class="ct-social-icon">
                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
              </span>
              <span class="ct-social-handle">DJ ReRe</span>
            </a>
          </div>
          <p class="ct-hashtag font-stroke">{site.hashtag}</p>
        </div>
      </div>

      <div class="ct-form-col" data-reveal style="--reveal-delay:160ms">
        <p class="ct-form-h">Prefer to share details now? Use our quick form.</p>
        <div class="ct-form-frame">
          <iframe
            id="quote-iframe"
            src="/quote-form.html"
            title="Request a quote"
            loading="eager"
            scrolling="no"
            style="width:100%;border:0;height:1500px;"
          ></iframe>
        </div>
        <p class="ct-secure">🔒 Submissions go straight to the REmixed Events team. We never share your details.</p>
      </div>
    </div>
  </section>

  <!-- FIND US + MAP -->
  <section class="section tight ct-map-section">
    <div class="container wide ct-map-grid">
      <div class="ct-map-copy">
        <p class="kicker" data-reveal>Find us</p>
        <h2 class="h2" data-reveal style="--reveal-delay:80ms">Find us in <span class="font-stroke gtext">Miami Beach.</span></h2>
        <p class="lede" data-reveal style="--reveal-delay:140ms">Serving South Florida and beyond.</p>
        <ul class="ct-details" data-reveal style="--reveal-delay:200ms">
          <li><span class="ct-d-k">Studio</span><span>{site.address}</span></li>
          <li><span class="ct-d-k">Email</span><a href={`mailto:${site.email}`}>{site.email}</a></li>
          <li><span class="ct-d-k">Call</span><a href={site.phoneHref}>{site.phone}</a></li>
          <li><span class="ct-d-k">Google</span><a href={site.reviewUrl} target="_blank" rel="noopener">Rated {site.googleRating}★ — read reviews</a></li>
        </ul>
      </div>
      <div class="ct-map" data-reveal style="--reveal-delay:120ms">
        <iframe
          title="REmixed Events location map"
          src={`https://www.google.com/maps?q=${encodeURIComponent(site.mapQuery)}&output=embed`}
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
        ></iframe>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <Faq items={contactFaqs} kicker="Before you book" heading="Quick questions, answered." />
</Base>

<style>
  .ct-hero { position: relative; padding-top: 130px; padding-bottom: 5rem; overflow: hidden; }
  .ct-glow { width: 50vw; height: 50vw; max-width: 620px; max-height: 620px; top: -12%; left: -10%; background: var(--grad-radial); opacity: 0.34; }
  .ct-grid {
    position: relative; z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 1rem + 4vw, 4.5rem);
    align-items: start;
  }
  .ct-copy { display: grid; gap: 1.4rem; position: sticky; top: 100px; }
  .ct-steps { display: grid; gap: 1.1rem; margin-top: 0.6rem; }
  .ct-step { display: flex; gap: 1rem; align-items: flex-start; }
  .ct-step-n { font-family: var(--font-display); font-weight: 700; font-size: 1.4rem; letter-spacing: -0.02em; flex-shrink: 0; min-width: 2.2rem; }
  .ct-step-t { font-family: var(--font-display); font-size: 1.1rem; font-weight: 600; letter-spacing: -0.01em; }
  .ct-step-d { color: var(--text-dim); font-size: 0.95rem; margin-top: 0.2rem; }
  .ct-sub { font-family: var(--font-display); font-weight: 600; font-size: clamp(1.1rem, 1rem + 0.6vw, 1.5rem); letter-spacing: -0.01em; color: var(--violet); }
  .ct-cta { padding-top: 0.3rem; }
  .ct-direct { display: flex; align-items: center; gap: 1.2rem; flex-wrap: wrap; padding-top: 0.5rem; }
  .ct-direct p { color: var(--text-mute); }
  .ct-badges { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .ct-form-h { font-size: 0.9rem; color: var(--text-mute); margin-bottom: 0.8rem; text-align: center; }

  .ct-capture { display: flex; gap: 0.6rem; flex-wrap: wrap; }
  .ct-capture input {
    flex: 1; min-width: 220px;
    padding: 0.95rem 1.1rem;
    border-radius: 999px;
    border: 1px solid var(--line-strong);
    background: rgba(255,255,255,0.6);
    color: var(--text);
    font: inherit;
  }
  .ct-capture input:focus { outline: none; border-color: var(--violet); background: #fff; }

  /* Find us + map */
  .ct-map-grid { display: grid; grid-template-columns: 0.9fr 1.1fr; gap: clamp(2rem, 1rem + 4vw, 4rem); align-items: center; }
  .ct-map-copy { display: grid; gap: 1.1rem; }
  .ct-details { display: grid; gap: 0.9rem; margin-top: 0.4rem; }
  .ct-details li { display: grid; grid-template-columns: 5.5rem 1fr; gap: 0.8rem; align-items: baseline; font-size: 0.98rem; color: var(--text-dim); }
  .ct-d-k { font-size: 0.72rem; letter-spacing: 0.18em; text-transform: uppercase; color: var(--text-mute); font-weight: 600; }
  .ct-details a { color: var(--text); }
  .ct-details a:hover { color: var(--magenta); }
  .ct-map { border-radius: 24px; overflow: hidden; padding: 1px; background: var(--grad-soft); box-shadow: 0 30px 70px -30px rgba(123, 47, 247, 0.45); }
  .ct-map iframe { display: block; width: 100%; height: clamp(320px, 42vh, 460px); border: 0; border-radius: 23px; filter: saturate(1.05); }
  @media (max-width: 880px) { .ct-map-grid { grid-template-columns: 1fr; } }
  /* On touch devices the Google Map's one-finger pan traps page scrolling.
     Disable its pointer capture so the whole Contact page scrolls freely. */
  @media (pointer: coarse) { .ct-map iframe { pointer-events: none; } }

  .ct-social { display: grid; gap: 0.7rem; padding-top: 1rem; margin-top: 0.6rem; border-top: 1px solid var(--line); justify-items: start; }
  .ct-social-h { font-family: var(--font-display); font-size: clamp(1.35rem, 1.1rem + 1vw, 1.8rem); font-weight: 600; letter-spacing: -0.01em; }
  .ct-social-copy { color: var(--text-dim); font-size: 0.98rem; max-width: 42ch; }
  .ct-social-copy strong { color: var(--violet); font-weight: 600; }
  .ct-social-grid { display: flex; gap: 1.4rem; flex-wrap: wrap; margin-top: 0.5rem; }
  .ct-social-item { display: grid; justify-items: center; gap: 0.55rem; text-align: center; color: var(--text); }
  .ct-social-icon {
    width: 54px; height: 54px;
    display: inline-flex; align-items: center; justify-content: center;
    border-radius: 50%;
    background: rgba(123, 47, 247, 0.07);
    border: 1px solid var(--line-strong);
    color: var(--text);
    transition: color 0.25s var(--ease), background 0.25s var(--ease), border-color 0.25s var(--ease), transform 0.25s var(--ease), box-shadow 0.25s var(--ease);
  }
  .ct-social-icon svg { width: 25px; height: 25px; fill: currentColor; }
  .ct-social-item:hover .ct-social-icon { color: #fff; background: var(--grad); border-color: transparent; transform: translateY(-3px); }
  .ct-social-handle { font-size: 0.85rem; color: var(--text-dim); font-weight: 500; }
  /* Instagram gets the spotlight */
  .ct-social-item-lead .ct-social-icon { width: 60px; height: 60px; color: #fff; background: var(--grad); border-color: transparent; box-shadow: 0 14px 30px -12px rgba(220, 30, 227, 0.6); }
  .ct-social-item-lead .ct-social-icon svg { width: 28px; height: 28px; }
  .ct-social-item-lead:hover .ct-social-icon { transform: translateY(-3px) scale(1.04); }
  .ct-social-item-lead .ct-social-handle { color: var(--text); font-weight: 700; }
  .ct-hashtag { font-size: 1.4rem; margin-top: 0.4rem; background: var(--grad-soft); -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; }

  .ct-form-frame {
    border-radius: 24px;
    padding: 1px;
    background: var(--grad-soft);
    box-shadow: 0 30px 70px -30px rgba(220, 30, 227, 0.5);
  }
  .ct-form-frame iframe { border-radius: 23px; display: block; background: var(--surface); }
  .ct-secure { text-align: center; color: var(--text-mute); font-size: 0.85rem; margin-top: 1rem; }

  @media (max-width: 880px) {
    .ct-grid { grid-template-columns: 1fr; }
    .ct-copy { position: static; }
  }
</style>

<script>
  // Auto-size the quote iframe to its content.
  window.addEventListener('message', (e) => {
    if (e?.data?.type === 'qf-height' && typeof e.data.height === 'number') {
      const frame = document.getElementById('quote-iframe') as HTMLIFrameElement | null;
      if (frame) frame.style.height = Math.max(600, e.data.height + 24) + 'px';
    }
  });

  // Quick email capture -> opens the visitor's mail client, pre-addressed to us.
  const capture = document.querySelector('[data-capture]') as HTMLFormElement | null;
  capture?.addEventListener('submit', (e) => {
    e.preventDefault();
    const input = capture.querySelector('input[type=email]') as HTMLInputElement | null;
    const email = input?.value?.trim();
    if (!email) return;
    const subject = encodeURIComponent('New inquiry from the REmixed website');
    const body = encodeURIComponent(`Hi REmixed team,\n\nI'd love to hear from you about my event.\nYou can reach me at: ${email}\n\nThanks!`);
    window.location.href = `mailto:info@remixedeventsllc.com?subject=${subject}&body=${body}`;
  });
</script>
