---
import type { Service } from '../data/site';
import Media from './Media.astro';
interface Props extends Service {
  delay?: number;
}
const { tag, title, blurb, points, image, alt, delay = 0 } = Astro.props;
---

<article class="card card-glow svc" data-reveal style={`--reveal-delay:${delay}ms`}>
  <Media src={image} alt={alt} aspect="16/10" feather={false} class="svc-thumb" />
  <div class="svc-top">
    <span class="svc-tag">{tag}</span>
    <span class="svc-mark gtext" aria-hidden="true">✦</span>
  </div>
  <h3 class="h3 svc-title">{title}</h3>
  <p class="svc-blurb">{blurb}</p>
  <ul class="svc-points">
    {points.map((p) => (
      <li><span class="svc-dot" aria-hidden="true"></span>{p}</li>
    ))}
  </ul>
</article>

<style>
  .svc {
    --pad: clamp(1.6rem, 1.2rem + 1vw, 2.2rem);
    padding: var(--pad);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    min-height: 100%;
  }
  .svc :global(.svc-thumb) {
    margin: calc(-1 * var(--pad)) calc(-1 * var(--pad)) 0.4rem;
    border-radius: 0;
    border-left: 0;
    border-right: 0;
    border-top: 0;
  }
  .svc-top { display: flex; align-items: center; justify-content: space-between; }
  .svc-tag {
    font-family: var(--font-display);
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--text-mute);
  }
  .svc-mark { font-size: 1.1rem; }
  .svc-title { margin-top: 0.2rem; }
  .svc-blurb { color: var(--text-dim); font-size: 0.98rem; flex: 1; }
  .svc-points { display: grid; gap: 0.5rem; margin-top: 0.4rem; }
  .svc-points li {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    font-size: 0.9rem;
    color: var(--text-dim);
  }
  .svc-dot {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--grad-soft);
    flex-shrink: 0;
  }
</style>
